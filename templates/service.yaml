apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-deployment.fullname" . }}
  labels:
    {{- include "hello-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-deployment.selectorLabels" . | nindent 4 }}
